server:
  port: 8083

spring:
  application:
    name: mtg-ai-orchestrator
  ai:
    ollama:
      base-url: ${OLLAMA_URL:http://localhost:11434}
      chat:
        options:
          model: ${OLLAMA_MODEL:llama3.2:3b}

app:
  market:
    api:
      url: ${MARKET_SERVICE_URL:http://localhost:8082}
